# Private live-moderation console; keep the existing value in Render.
ADMIN_KEY=
# Cleanup-only access to OLD archived rows from the retired seven-day archive.
# Keep until expiry cleanup reports remaining: 0; cannot enable new storage.
ARCHIVE_DATABASE_URL=
ARCHIVE_CLEANUP_TOKEN=
# ARCHIVE_ENCRYPTION_KEY and old VAPID variables are no longer read.
# Never put actual credentials in this example file.
