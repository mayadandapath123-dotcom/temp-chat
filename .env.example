# Set these in Render Environment (generate locally; never commit real keys).
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:your-real-email@example.com
# OPTIONAL: only with an explicitly provisioned persistent disk, not free ephemeral storage.
# PUSH_STORE_PATH=/var/data/tempchat-push-subscriptions.json
