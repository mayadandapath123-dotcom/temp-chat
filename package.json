{
  "name": "temp-chat",
  "version": "1.0.0",
  "description": "Ephemeral private temporary chat",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/*.test.js",
    "push:keys": "node scripts/generate-push-keys.js",
    "admin:key": "node scripts/generate-admin-key.js",
    "archive:secrets": "node scripts/generate-archive-secrets.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pg": "^8.23.0",
    "socket.io": "^4.7.5"
  },
  "devDependencies": {
    "socket.io-client": "^4.8.4"
  }
}
